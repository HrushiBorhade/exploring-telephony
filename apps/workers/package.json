{
  "name": "@repo/workers",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/worker.ts",
    "build": "tsc",
    "start": "node dist/worker.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@google/genai": "*",
    "@repo/db": "workspace:*",
    "@repo/queues": "workspace:*",
    "bullmq": "*",
    "csv-stringify": "*",
    "pino": "*",
    "pino-pretty": "*",
    "dotenv": "*"
  },
  "devDependencies": {
    "@types/node": "*",
    "tsx": "*",
    "typescript": "*",
    "vitest": "*"
  }
}
